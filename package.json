{
  "name": "publisher",
  "version": "0.1.0",
  "description": "AWS Lambda for publish hugo generated static content",
  "main": "lambda.js",
  "scripts": {
    "compile": "tsc && node env.js",
    "lambda:create": "node dist/cmd/lambda.create.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zhikiri <zhikiri.dev@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^11.13.6",
    "aws-sdk": "^2.441.0",
    "dotenv": "^7.0.0"
  },
  "dependencies": {}
}
